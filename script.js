let flashcards=['What is CPU?|Central Processing Unit','HTML stands for?|HyperText Markup Language'];let fi=0;function nextF(){fi=(fi+1)%flashcards.length;document.getElementById('card').innerText=flashcards[fi].split('|')[0];}document.addEventListener('DOMContentLoaded',()=>{document.getElementById('card')&&(document.getElementById('card').innerText=flashcards[0].split('|')[0])});